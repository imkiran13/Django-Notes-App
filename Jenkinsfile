pipeline {
    agent { label "jenkins-slave"}

    stages {
        stage('Hello') {
            steps {
                echo 'creating folder'
                sh 'mkdir djando-folder'
            }
        }
    }
}

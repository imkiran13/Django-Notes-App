pipeline {
    agent { label "jenkis-slave"}

    stages {
        stage('Hello') {
            steps {
                echo 'creating folder'
                sh 'mkdir djando-folder'
            }
        }
    }
}
